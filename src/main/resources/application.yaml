
spring:
  datasource:
    url: ...
    username: ...
    password: ...
    driver-class-name: org.postgresql.Driver
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ...
            client-secret: ...
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
liquibase:
enabled: false
default-schema: forex
security:
  jwt:
    secret: ...
    access: 3600000
    refresh: 2592000000
logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
server:
  port: 8080